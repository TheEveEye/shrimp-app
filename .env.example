###############################################
# SHRIMP Web App – Environment Example (Vite) #
###############################################

# Copy this file to `.env` and fill in values below.
# Note: This app uses EVE SSO as a public client with PKCE.
# No client secret is used on the frontend-only flow.

# === EVE SSO (PKCE public client) ===
VITE_EVE_SSO_ISSUER=https://login.eveonline.com/v2
VITE_EVE_CLIENT_ID=YOUR_PUBLIC_CLIENT_ID
VITE_EVE_REDIRECT_URI=http://localhost:5173/auth/callback
# For production, set to your deployed URL:
# VITE_EVE_REDIRECT_URI=https://<your-domain>/auth/callback
# Space-separated list of scopes (include 'openid' to receive an id_token)
VITE_EVE_SCOPES=openid esi-location.read_location.v1 esi-location.read_ship_type.v1 esi-ui.write_waypoint.v1
# Optional; only include if your validation requires it
VITE_EVE_AUDIENCE=EVE Online
# No client secret for PKCE public client (placeholder for future server-side flow)
# VITE_EVE_CLIENT_SECRET=

# === App metadata ===
VITE_APP_NAME=SHRIMP

# === Existing app config ===
# Loaded in production mode (vite build/preview)
VITE_APP_ENV=production
VITE_API_URL=https://api.kiwiapps.dev
